{
  "name": "@four-bytes/four-opencode-git-tools",
  "version": "0.1.1",
  "description": "Git history analysis tools for opencode agents — curse scores, bus factor, implicit coupling, and ownership",
  "license": "Apache-2.0",
  "type": "module",
  "main": "dist/four-opencode-git-tools.js",
  "scripts": {
    "build": "bun build src/four-opencode-git-tools.ts --outdir=dist --target=bun --external @opencode-ai/plugin",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "four-bytes",
    "git",
    "git-history",
    "code-analysis"
  ],
  "dependencies": {
    "@opencode-ai/plugin": "^1.17.7"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "typescript": "^5.7.3"
  }
}
